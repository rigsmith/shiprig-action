---
"shiprig-action": minor
---

The `pr-status` sub-action runs on shiprig: its comment shows each package's new version and a changelog preview of the pull request's own entries, in any ecosystem shiprig supports. It no longer needs `@changesets/cli`.
